/**
 * 布局模式：
 * - sidebar    侧边栏通高
 * - mixed      顶部通宽 + 左侧边栏
 * - two-column 双列：左侧图标轨道（一级）+ 第二列子菜单
 */
export type LayoutMode = 'sidebar' | 'mixed' | 'two-column';

/** 配色方案 */
export type ColorScheme = 'light' | 'dark';
