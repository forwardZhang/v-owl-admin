/** 侧边栏展开宽度（px） */
export const SIDEBAR_WIDTH = 220;

/** 侧边栏折叠宽度（px） */
export const SIDEBAR_COLLAPSED_WIDTH = 88;

/** 顶栏 / 头部高度（px），与 CSS 变量 --app-header-height 保持一致 */
export const HEADER_HEIGHT = 64;
